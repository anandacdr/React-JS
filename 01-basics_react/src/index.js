import React from "react"; // Core Foundation
import ReactDOM from "react-dom/client"; // Implementation of Web

import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);  
